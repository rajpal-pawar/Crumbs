[workspace]
resolver = "2"
members = [
    "crumbs-daemon",
    "src-tauri",
]

# ---------------------------------------------------------------------------
# Shared profile overrides
# ---------------------------------------------------------------------------
# Keep debug builds fast for iteration; release builds lean and fast.
[profile.dev]
opt-level = 0
debug = true

# Optimize all dependencies even in debug mode.
# This makes ONNX model loading take <1s instead of 3+ minutes during development!
[profile.dev.package."*"]
opt-level = 3

[profile.release]
opt-level = 3
lto = "thin"
codegen-units = 1
strip = "debuginfo"

# A dedicated profile for CI / quick smoke-checks that still catches panics.
[profile.ci]
inherits = "release"
debug = true
strip = "none"
